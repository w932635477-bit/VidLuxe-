# UI 升级设计文档：从"高级感"到"种草力"

> 版本：v1.0
> 日期：2026-02-23
> 状态：已批准，待实现

---

## 一、背景

根据需求文档 v3.0 的战略升级，项目定位从"高级感引擎"转变为"种草内容增强工具"。本设计文档描述 UI 层面的升级方案。

## 二、目标

1. **体验页流程升级** - 增加 AI 识别步骤，让用户确认品类和种草类型
2. **评分系统升级** - 从 4 维高级感评分 → 5 维种草力评分
3. **首页文案升级** - 从"高级感"定位 → "种草力"定位
4. **保持设计风格** - Apple 风格、纯黑背景、香槟金品牌色

## 三、用户流程设计

### 3.1 新的 5 步流程

```
上传 → AI识别 → 风格 → 处理 → 结果
[1]    [2]     [3]    [4]    [5]
```

### 3.2 各步骤说明

| 步骤 | 名称 | 说明 | 变化 |
|------|------|------|------|
| 1 | 上传 | 拖拽/点击上传图片或视频 | 保持不变 |
| 2 | AI 识别 | AI 自动分析，展示品类+种草类型建议 | **新增** |
| 3 | 风格选择 | 根据品类+种草类型推荐风格 | 调整推荐逻辑 |
| 4 | 处理中 | 显示进度和当前阶段 | 保持不变 |
| 5 | 结果 | Before/After 对比 + 种草力评分 | **评分系统升级** |

## 四、数据结构设计

### 4.1 新增类型定义

```typescript
// 品类类型 (8种)
type CategoryType =
  | 'fashion'   // 穿搭
  | 'beauty'    // 美妆
  | 'food'      // 美食
  | 'cafe'      // 探店
  | 'home'      // 家居
  | 'travel'    // 旅行
  | 'tech'      // 数码
  | 'fitness';  // 健身

// 种草类型 (3种)
type SeedingType =
  | 'product'      // 种草商品 - 让读者想买
  | 'location'     // 种草地点 - 让读者想去
  | 'lifestyle';   // 种草生活方式 - 让读者想成为

// AI 识别结果
interface AIRecognitionResult {
  category: CategoryType;
  categoryConfidence: number;  // 置信度 0-1
  seedingType: SeedingType;
  seedingTypeConfidence: number;
  suggestedStyles: StyleType[];  // 推荐风格
}

// 种草力评分 (5维)
interface SeedingScore {
  overall: number;           // 总分 0-100
  grade: 'S' | 'A' | 'B' | 'C' | 'D';
  dimensions: {
    visualAttraction: number;    // 视觉吸引力 30%
    contentMatch: number;        // 内容匹配度 25%
    authenticity: number;        // 真实可信度 20%
    emotionalImpact: number;     // 情绪感染力 15%
    actionGuidance: number;      // 行动引导力 10%
  };
}
```

### 4.2 品类配置

```typescript
const CATEGORIES: CategoryConfig[] = [
  { id: 'fashion', label: '穿搭', icon: '👗' },
  { id: 'beauty', label: '美妆', icon: '💄' },
  { id: 'food', label: '美食', icon: '🍽️' },
  { id: 'cafe', label: '探店', icon: '☕' },
  { id: 'home', label: '家居', icon: '🏠' },
  { id: 'travel', label: '旅行', icon: '✈️' },
  { id: 'tech', label: '数码', icon: '📱' },
  { id: 'fitness', label: '健身', icon: '💪' },
];
```

### 4.3 种草类型配置

```typescript
const SEEDING_TYPES: SeedingTypeConfig[] = [
  {
    id: 'product',
    label: '种草商品',
    description: '让读者想买',
    enhancementFocus: '突出产品、展示细节、强调质感',
  },
  {
    id: 'location',
    label: '种草地点',
    description: '让读者想去',
    enhancementFocus: '突出氛围、场景感、代入感',
  },
  {
    id: 'lifestyle',
    label: '种草生活方式',
    description: '让读者想成为',
    enhancementFocus: '突出理想感、真实感、共鸣感',
  },
];
```

## 五、组件设计

### 5.1 新增组件

| 组件 | 路径 | 说明 |
|------|------|------|
| `AIRecognitionStep` | `components/features/try/AIRecognitionStep.tsx` | Step 2: AI 识别与确认 |
| `CategorySelector` | `components/features/try/CategorySelector.tsx` | 品类选择器 |
| `SeedingTypeSelector` | `components/features/try/SeedingTypeSelector.tsx` | 种草类型选择器 |
| `SeedingScoreCard` | `components/features/try/SeedingScoreCard.tsx` | 种草力评分卡片 |

### 5.2 修改组件

| 组件 | 路径 | 变化 |
|------|------|------|
| `TryPage` | `app/try/page.tsx` | 新增 Step 2，调整流程 |
| `StepIndicator` | `app/try/page.tsx` (内联) | 5 步指示器 |
| `StyleSelector` | `components/features/try/StyleSelector.tsx` | 根据品类+种草类型推荐 |

## 六、UI 设计规范

### 6.1 保持的设计风格

- **背景色**: 纯黑 `#000000`
- **品牌色**: 香槟金 `#D4AF37`
- **字体**: Playfair Display (标题) + Plus Jakarta Sans (正文)
- **圆角**: 按钮 `980px`，卡片 `24px`
- **动画**: Apple 缓动曲线 `cubic-bezier(0.25, 0.1, 0.25, 1)`

### 6.2 新增评分颜色

```css
/* 种草力评分等级色 (复用现有) */
--grade-s: #FFD700;  /* 顶级 - 金色 */
--grade-a: #34C759;  /* 优秀 - 绿色 */
--grade-b: #007AFF;  /* 良好 - 蓝色 */
--grade-c: #FF9500;  /* 普通 - 橙色 */
--grade-d: #FF3B30;  /* 需改进 - 红色 */
```

## 七、实现计划

### Phase 1: 体验页流程升级

1. 新增类型定义 `lib/types/seeding.ts`
2. 新增品类和种草类型配置 `lib/config/seeding.ts`
3. 新增 `CategorySelector` 组件
4. 新增 `SeedingTypeSelector` 组件
5. 新增 `AIRecognitionStep` 组件 (Step 2)
6. 修改 `TryPage` 流程为 5 步
7. 更新 `StepIndicator` 为 5 步

### Phase 2: 评分系统升级

1. 新增 `SeedingScore` 类型
2. 新增 `SeedingScoreCard` 组件
3. 更新 API 响应类型
4. 更新结果页面展示

### Phase 3: 首页文案升级

1. 更新 Hero 区文案
2. 更新功能区文案
3. 更新 CTA 文案

## 八、验收标准

- [ ] 体验页流程从 4 步变为 5 步
- [ ] Step 2 展示 AI 识别结果，用户可确认或修改
- [ ] 评分系统从 4 维变为 5 维种草力评分
- [ ] 首页文案从"高级感"变为"种草力"
- [ ] 保持 Apple 风格设计不变

---

> 设计批准时间：2026-02-23
> 下一步：调用 writing-plans 技能创建详细实现计划
